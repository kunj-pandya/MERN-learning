<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-600 rounded-md px-3 py-2 text-sm" href="/logout">logout</a>
        </div>
        <h3 class="mb-5 text-3xl"><span class="font-light">Hello</span>, <%= user.name %>
        </h3>
        <h5 class="mb-5">you can crate a new post.</h5>
        <form action="/post" method="post">
            <textarea placeholder="what's in your mind?"
                class="resize-none bg-transparent border-2 border-zinc-800 rounded-md w-1/3 p-3 block"
                name="content"></textarea>
            <input type="submit" class="px-3 py-2 bg-blue-600 block rounded-md mt-2 w-40 text-sm"
                value="create new post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postContainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-3 mb-4 border-[1] border-zinc-700 rounded-md bg-zinc-800">
                        <h4 class="text-blue-600 mb-2">
                            <%= user.username %>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small class="mt-1 inline-block">
                            <%= post.likes.length %> likes
                        </small>
                        <div class="btns flex gap-2 mt-2">
                            <a class="text-blue-600" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)===-1 ? "like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%=post._id %>" class="text-zinc-600">edit</a>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>

    </div>

</body>

</html>